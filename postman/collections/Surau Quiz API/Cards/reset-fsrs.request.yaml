$kind: http-request
name: Reset FSRS
method: PUT
url: '{{base_url}}/api/v1/cards/{{card_id}}/reset'
order: 4500
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: card_id
    value: '{{card_id}}'
