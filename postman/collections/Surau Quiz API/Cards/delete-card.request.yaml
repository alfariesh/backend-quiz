$kind: http-request
name: Delete Card
method: DELETE
url: '{{base_url}}/api/v1/cards/{{card_id}}'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: card_id
    value: '{{card_id}}'
