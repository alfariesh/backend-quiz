$kind: http-request
name: Delete Goal
method: DELETE
url: '{{base_url}}/api/v1/goals/{{goal_id}}'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: goal_id
    value: '{{goal_id}}'
