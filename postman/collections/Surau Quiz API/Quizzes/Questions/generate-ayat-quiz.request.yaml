$kind: http-request
name: Generate Ayat Quiz
method: POST
url: '{{base_url}}/api/v1/quizzes/{{quiz_id}}/questions/generate-ayat'
order: 4000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: quiz_id
    value: '{{quiz_id}}'
body:
  type: json
  content: |-
    {
      "deck_id": "{{deck_id}}",
      "question_type": "ayat_cloze",
      "count": 10
    }
