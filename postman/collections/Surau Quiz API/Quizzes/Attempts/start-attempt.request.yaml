$kind: http-request
name: Start Attempt
method: POST
url: '{{base_url}}/api/v1/quizzes/{{quiz_id}}/attempts'
order: 1000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: quiz_id
    value: '{{quiz_id}}'
