$kind: http-request
name: Export Deck
method: GET
url: '{{base_url}}/api/v1/decks/{{deck_id}}/export'
order: 9000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: deck_id
    value: '{{deck_id}}'
