$kind: http-request
name: Get Deck
method: GET
url: '{{base_url}}/api/v1/decks/{{deck_id}}'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: deck_id
    value: '{{deck_id}}'
