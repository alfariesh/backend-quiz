$kind: http-request
name: Refresh Token
method: POST
url: '{{base_url}}/api/v1/auth/refresh'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "refresh_token": "{{refresh_token}}"
    }
