$kind: http-request
name: End Study Session
method: PUT
url: '{{base_url}}/api/v1/study/sessions/{{session_id}}/end'
order: 4000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: session_id
    value: '{{session_id}}'
