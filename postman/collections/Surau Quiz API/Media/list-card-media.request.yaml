$kind: http-request
name: List Card Media
method: GET
url: '{{base_url}}/api/v1/cards/{{card_id}}/media'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{access_token}}'
pathVariables:
  - key: card_id
    value: '{{card_id}}'
